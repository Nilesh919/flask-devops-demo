apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 32450
  selector:
    app: {{ .Release.Name }}-flask-devops-demo
